[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "normal"
version = "0.1.0"
description = "Local workbench for taming pirated music and movie libraries"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "mutagen>=1.47",
  "openpyxl>=3.1",
  "Pillow>=10.0",
]
authors = [
  { name = "Lachlan" }
]

[project.scripts]
normal = "normal.cli:main"

[tool.setuptools]
packages = ["normal"]
